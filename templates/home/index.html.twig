{% extends 'base.html.twig' %}

{% block body %}

    <div 
        class="pb-9"
        {{ stimulus_controller('reload-content',  {
            url: path('app_dashboard_refresh'),
            addRouteUrl: path('app_route_addOrder'),
            routeSelectedId: routeSelected.id
        }) }}
        data-action="modal-form:success->reload-content#refreshContent sortable:addedOrder->reload-content#addOrderToRoute"
    >
        <div 
            data-reload-content-target="content" 
            {{ stimulus_controller('route-dashboard') }}>    
             {{ include('home/_homeDashboard.html.twig')}}
        </div> 
    </div>

{% endblock %}  